# useEffect
   executes asynchronously after the initial render paints, which can cause layout flickering if updating structural state.

when there is a competition b/w initial rendering

# context API
  A built-in React dependency injector that shares values down the component tree but triggers re-renders on all consumer components when values change.

# State Management
    The architectural process of storing, updating, and synchronizing user and application data across a software ecosystem.

# Advanced State management with Zustand
  A high-performance, boilerplate-free global state library that selects specific state slices to prevent unnecessary component re-renders without using context providers.