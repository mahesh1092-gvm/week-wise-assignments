### frontend:
  # HTML-> HyperTextMarkupLanguage

  -> elements: html>, head> title> body> li> ol> dl> u> img>
    for img> we require the referece of image which is to be added to the page inside: href:""
  -> list: orderd, unordered, and description lists
